policy_version = 2

[output]
mode = "enforce"
fail_threshold = 80

[weights]
test_gap_max_penalty = 45
dangerous_change_max_penalty = 55
dependency_update_max_penalty = 35

[test_gap]
missing_tests_penalty = 34
large_change_lines = 120
large_change_penalty = 11

[dangerous_change]
per_file_penalty = 14
critical_bonus_penalty = 10

[dependency_update]
manifest_penalty = 24
lockfile_penalty = 12
large_lockfile_churn = 120
large_lockfile_penalty = 16
